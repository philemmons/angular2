let logFn = function(message){
    console.log(message);
}
// TypeScript function declaration
// also known as a 'arrow function' or lamda expression
// with one line in the code block, exclude the {}'s
// furthermore, with only one parameter the {} can be excluded
// BUT it makes it hard to read. 
let doLog = (message) => console.log(message);